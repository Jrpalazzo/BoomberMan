Generate grid pattern for the solid unbreakable walls (simply two nested for-loops), and then fill the rest randomly (using a pseudo-random-generator, such as rand()), making sure to leave the three cells in the top left corner empty.?